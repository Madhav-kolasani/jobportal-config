server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${JOB_DB_NAME:job_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:MySQL@123}
    driver-class-name: com.mysql.cj.jdbc.Driver

springdoc:
  swagger-ui:
    enabled: false
